name: bio-hfl
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - pip
  - pytorch
  - torchvision
  - pytorch-cuda=11.8
  - pip:
      - numpy
      - pandas
      - matplotlib
      - scikit-learn
      - Pillow
      - tqdm
